// use halo2wrong::curves::bn256::Bn256;
// use halo2wrong::halo2::poly::{commitment::Params, kzg::commitment::ParamsKZG};
// use rand::{rngs::OsRng, thread_rng, Rng};
// use std::{env, fs::File, io::BufReader, marker::PhantomData};

pub fn main() {
    // let args: Vec<String> = env::args().collect();
    // let k = u32::from_str_radix(&args[1], 10).unwrap();
    // let filename = &args[2];
    // let mut params_file = File::create(filename).unwrap();
    // let params = ParamsKZG::<Bn256>::setup(k, OsRng);
    // params.write(&mut params_file).unwrap();
}
